<UserControl x:Class="WinHAB.Desktop.Controls.TaskProgressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             x:Name="TaskProgressControlName"
             Visibility="{Binding TaskProgress.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
             HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid Name="Progress">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <mui:ModernProgressRing IsActive="True" Width="40" Height="40" HorizontalAlignment="Center" Style="{StaticResource ThreeBounceProgressRingStyle}" />
        <TextBlock Grid.Row="1" Text="{Binding TaskProgress.Text, ElementName=TaskProgressControlName}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
