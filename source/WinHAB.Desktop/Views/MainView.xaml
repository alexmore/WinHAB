<UserControl x:Class="WinHAB.Desktop.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:WinHAB.Desktop.Controls"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Name="Header" Grid.Row="0" Background="{DynamicResource WindowBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Name="LogoBorder" Background="{DynamicResource Accent}" BorderThickness="0" Grid.RowSpan="2">
                <Path Data="{StaticResource OpenHabIcon}" Fill="White" Stretch="Uniform" Width="30" Height="30" Margin="5"/>
            </Border>
            
            <Grid Name="HeaderPanel" Grid.Row="0" Grid.Column="1" Margin="10,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                <controls:IconLinkButton x:Name="HeaderButton"
                                         IconData="{StaticResource ArrowHeadDownIcon}" IconSize="12"
                                         Content="DEMO HOUSE" Style="{StaticResource RightIconLinkButton}"
                                         FontSize="16" FontWeight="Bold"
                                         Click="OpenHeaderDropDown"/>
                <Popup Name="HeaderDropDownPopup" StaysOpen="False"
                       PlacementTarget="{Binding ElementName=HeaderButton}" >
                    <Border Background="{DynamicResource WindowBackground}" BorderThickness="1" BorderBrush="{DynamicResource Accent}" Padding="10" >
                            <controls:HeaderDropDownControl AppConfiguration="{Binding AppConfiguration}" NavigationService="{Binding NavigationService}" />
                    </Border>
                </Popup>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
